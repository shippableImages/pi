NAME

      gcloud config list - view Google Cloud SDK properties

SYNOPSIS

      gcloud config list [SECTION/PROPERTY] [--all] [GLOBAL-FLAG ...]

DESCRIPTION

      List all currently available Cloud SDK properties associated with your
      current workspace or global configuration.

POSITIONAL ARGUMENTS

      [SECTION/PROPERTY]

         The property to be listed. Note that SECTION/ is optional while
         referring to properties in the core section.

FLAGS

      --all

         List all set and unset properties that match the arguments.

GLOBAL FLAGS

      Run $ gcloud help for a description of flags available to all commands.

EXAMPLES

      To list the project property in the core section, run:

        $ gcloud config list project

      To list the zone property in the compute section, run:

        $ gcloud config list compute/zone

      To list all the properties, run:

        $ gcloud config list --all

NOTES

      This command is in the Google Cloud SDK core component. See installing
      components if it is not installed.

